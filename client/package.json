{
  "name": "kafka-openshift-nodejs-sparkline",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "patternfly": "^3.54.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4",
    "serve": "^10.0.0"
  },
  "scripts": {
    "start": "react-scripts build && serve -s -l tcp://0.0.0.0:8080 build",
    "build": "npm install",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
